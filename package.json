{
  "name": "pi-r2-share",
  "version": "1.0.0",
  "description": "Share pi sessions via Cloudflare R2 instead of GitHub Gist/pi.dev",
  "keywords": ["pi-package"],
  "type": "module",
  "scripts": {
    "test": "bun test tests/"
  },
  "pi": {
    "extensions": ["./src/index.ts"]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "^0.75.4"
  },
  "trustedDependencies": [
    "koffi",
    "protobufjs"
  ],
  "dependencies": {
    "@earendil-works/pi-tui": "^0.75.4"
  }
}